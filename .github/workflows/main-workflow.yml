name: Build and Run

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: setup and run linter
      run: |
        pip install cpplint
        cpplint  --filter=-legal --linelength=100 *.cpp

    - name: setup cmake
      run: sudo apt install cmake

    - name: tshirts fail
      run: |
        g++ -Wall -Wextra -Werror src/tshirts_size_conversion.cpp test/tshirts_size_conversion_test.cpp -o tshirts_size_conversion_test.out
        bash .github/workflows/expectfail.sh build/tshirts_size_conversion_test.out

    - name: misaligned fail
      run: |
        g++ -Wall -Wextra -Werror misaligned.cpp -o misaligned.out
        bash .github/workflows/expectfail.sh ./misaligned.out

    - name: alerter fail
      run: |
        g++ -Wall -Wextra -Werror weatherreport.cpp -o weatherreport.out
        bash .github/workflows/expectfail.sh ./weatherreport.out
